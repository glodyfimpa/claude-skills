---
id: t1778606943593
title: Implementa pa-form-dynamic-id-mapper — strategia label-first + catalog fallback + auto-save
status: done
---

## Description
Sub-modulo di pa-form-filler che risolve i campi con ID dinamici (Keycloak: name=field_a3f7b cambia a ogni sessione). Strategia hybrid: label-first funziona su ~90% portali PA / catalog fallback per portali noti / discovery manuale + auto-save quando entrambe falliscono. Sequenziale a T3 perché modifica lo stesso pa-form-filler/SKILL.md.

## Acceptance Criteria
- Strategia label-first documentata in SKILL.md: scan DOM — match <label> — risale al campo via attributo for o posizione DOM
- Catalog fallback: lookup in portals-catalog.yaml quando label-first fallisce
- Auto-save: discovery manuale con conferma utente → nuova regola scritta nel YAML per usi futuri
- Test bats coprono: label-first su DOM fixture HTML — fallback catalog hit — auto-save di una regola nuova nel YAML (con cleanup teardown)
